# 개발 지침 (python-daouoffice-bot)

이 저장소에서 작업할 때 반드시 지키는 규칙. (요약: **코드는 영어, 문서는 한국어, 봇은 백그라운드 데몬.**)

## 언어

- **코드는 영어.** 식별자, 주석, docstring, 로그 메시지, 예외/에러 메시지, CLI 출력 문자열은 모두 영어로 쓴다. 한 표면 안에서 한/영을 섞지 않는다.
- **문서는 한국어.** README, `docs/**`, `ARCHITECTURE.md`, `CHANGELOG.md`, 스킬 문서는 한국어.
- **예외:** 봇이 실제로 보내는 *콘텐츠* 문자열(예제의 한국어 답장 등)은 데이터이므로 한국어 허용.

## 문서

- 하드 워드랩 금지. 문단/리스트 항목/blockquote = 한 줄, 렌더러가 줄바꿈. 코드펜스·표·mermaid·frontmatter는 그대로 보존.
- 아키텍처/개발가이드 문서는 **항상 현재 코드 상태만** 기술한다. "예전엔 X였다 Y로 바꿈" 같은 변경 이력 서술 금지 — 이력은 git/CHANGELOG의 몫. (CHANGELOG는 정의상 이력 문서라 예외.)

## 봇 런타임 / 설정 모델

- 메신저 봇은 **백그라운드 데몬**이다. 토큰(~30분) 만료 시 **기억한 자격증명으로 스스로 재로그인**해야 한다 — 사용자에게 재로그인을 요구하지 않는다.
- 온보딩: `daoubot login` → `~/.daoubot/profile.json`(홈 디렉터리 기준, 실행 cwd 무관 — `~/.aws`/`~/.docker` 류 관례)(테넌트·신원·세션 토큰·**비밀번호**). 데몬이 무인 자동 재로그인하려면 비밀번호가 필요하므로 프로필에 저장한다 — 파일은 `chmod 600`·`.daoubot/` gitignore, 그리고 **stdout 출력 시에만 `****` 마스킹**. 한 호스트에 봇 여러 개는 `--config`/`config_path` 로 프로필 파일을 분리한다.
- `DaouBot()` 은 인자 없이도 동작: 모든 연결값(비밀번호 포함)을 **명시 인자 > `DAOU_*` env > profile.json** 순으로 해석한다. 별도 API(`from_env` 류)는 두지 않고 env 오버라이드는 *문서로만* 안내한다.
- 토큰(~30분) 만료 시 저장된 비밀번호로 무한 자동 재로그인하고, 새 토큰을 프로필에 다시 저장한다. 비밀번호가 전혀 없으면 명확한 에러로 멈춘다(사용자에게 재로그인을 강요하지 않음).

## 보안

- 자격증명/토큰을 커밋·argv·stdout 에 평문으로 노출하지 않는다. 실제 시크릿·사내 식별자·실명·`.saz` 캡처는 저장소에 두지 않는다(플레이스홀더만).
- 비밀번호는 로컬 `~/.daoubot/profile.json`(gitignore·`chmod 600`) 에만 저장하고, 화면 출력 시 `****` 로 마스킹한다(`Profile.public_dict`). 저장소·예제·커밋 어디에도 실제 비밀번호 금지.
- 역분석 산출물의 계약은 "SAZ 기반·라이브 미검증"임을 정직히 표기한다.

## 커밋 전 게이트

```bash
uv run ruff check .
uv run ruff format --check .
uv run pytest -q
```

전부 통과해야 커밋. 예제는 `python -m py_compile`로 깨지지 않는지 확인한다.
